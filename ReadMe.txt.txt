Initialize the Backend application:-
1. Install all dependencies
              > npm i body-parser express mongoose

Initialize the Frontend application:-
1. Install the dependencies
              > npm i @fontsource/inter axios express fs react react-dom react-icons react-router-dom react-router-hash-link react-scroll react-slick slick-carousel


At first no skill card would be visible you will have to add the skills using add skill button. As soon as you add the skill it will be visible in the browser.